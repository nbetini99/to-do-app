
export type Task = {
    name: string;
    desc: string;
    duedate: string;
    id: string;
    todoId: string;
    completed: boolean;
}

export type Todo = {
    name: string;
    color: string;
    id: string
}